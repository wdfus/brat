<UserControl x:Class="Brat.UserRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Brat"
             FontFamily="pack://application:,,,/Brat;component/Fonts/#SF Pro Rounded"
             mc:Ignorable="d" Height="60"
             Cursor="Hand">
    <UserControl.Resources>
        <SolidColorBrush x:Key="TickBackColor" Color="#FF9E9E9E"/>
        <!-- серый -->
        <SolidColorBrush x:Key="TickFrontColor" Color="#FF9E9E9E"/>
        <!-- по умолчанию серый -->
        <SolidColorBrush x:Key="TickReadColor"  Color="#FF2EA6FF"/>
    </UserControl.Resources>
    <Grid Height="60" Margin="-6, 0, 0, 0" Width="301" x:Name="gridFather">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="52"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Ellipse Width="42" Height="42" Fill="#D9D9D9" VerticalAlignment="Center" Margin="5,0,0,0" x:Name="TagToUserId"/>
        <StackPanel Grid.Column="1" Margin="5,0,0,0" VerticalAlignment="Center" Orientation="Horizontal">
            <StackPanel Width="190">
                <StackPanel Height="15" Width="200" HorizontalAlignment="Left">
                    <TextBlock x:Name="nameOfUser" FontSize="14" Foreground="#FFE2E2E2"/>
                </StackPanel>
                <StackPanel Height="15" Width="200" Margin="0, 5, 0, 0" HorizontalAlignment="Left">
                    <TextBlock x:Name="LastUserText" FontSize="14" FontWeight="Normal" Foreground="#FFA4A4A4"/>
                </StackPanel>
            </StackPanel>
            <Viewbox Width="18" Height="12" VerticalAlignment="Top" x:Name="CheckArrow">
                <Canvas Width="20" Height="18">
                    <Path Data="M2 12 L8 18 L18 6" x:Name="FirstArrow"
                  Stroke="{DynamicResource TickBackColor}"
                  StrokeThickness="2"
                  StrokeLineJoin="Round"
                  StrokeStartLineCap="Round"
                  StrokeEndLineCap="Round"
                  Opacity="0.9"
                  Fill="Transparent"/>

                    <Path Data="M6 12 L12 18 L22 6" x:Name="SecondArrow"
                  Stroke="{DynamicResource TickFrontColor}"
                  StrokeThickness="2"
                  StrokeLineJoin="Round"
                  StrokeStartLineCap="Round"
                  StrokeEndLineCap="Round"
                  Fill="Transparent"/>
                </Canvas>
            </Viewbox>

            <TextBlock x:Name="TimeTextBox" Text="16:57" FontSize="14" Background="{x:Null}" VerticalAlignment="Top" Foreground="#FF717171"/>
            <StackPanel/>
        </StackPanel>
    </Grid>
</UserControl>
