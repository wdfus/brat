<Window x:Class="Brat.CaptionPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        PreviewKeyDown="Window_PreviewKeyDown"
    WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        Topmost="True" 
        FontFamily="pack://application:,,,/Brat;component/Fonts/#SF Pro Rounded" KeyDown="Window_KeyDown" Loaded="Window_Loaded"  >


    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>

        </Style>
    </Window.Resources>

    <Border Background="#2E2E2E"
            CornerRadius="20"
            Padding="20"
            Width="500">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.ColumnSpan="4" x:Name="Contanier">
                <TextBlock FontSize="12" Foreground="White" Text="Отправить как файл" HorizontalAlignment="Left" FontWeight="Bold" Margin="0,0,0,40" x:Name="SendHowTextBlock"/>
                <Image Source="/Fonts/x.png" x:Name="CapturePlace" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                <StackPanel x:Name="VanyaChmo" Visibility="Collapsed">
                    <TextBlock FontSize="12" Foreground="White" Text="Название файла.Xlsx" HorizontalAlignment="Center"/>
                    <Separator Margin="0, 8" x:Name="SendHowFileSeparator"/>
                    <TextBlock FontSize="12" Foreground="White" Text="Название файла.Xlsx" HorizontalAlignment="Center"/>
                    <Separator Margin="0, 8"/>
                </StackPanel>
            </StackPanel>
            <Border BorderThickness="0,0,0,1" BorderBrush="Gray" Grid.Row="1" Grid.ColumnSpan="4" Margin="0, 0, 0, 15">
                <StackPanel>
                    <TextBlock Text="Подпись" Foreground="White" FontWeight="Bold" Margin="0, 15, 0, 0"/>
                    <xctk:WatermarkTextBox  x:Name="CaptionTextBox" BorderThickness="0" Background="Transparent" Foreground="White"
                 Padding="10" FontSize="14">
                        <xctk:WatermarkTextBox.WatermarkTemplate>
                            <DataTemplate>
                                <TextBlock
                    Text="{Binding}"
                    Foreground="#FFD9D9D9"
                    Opacity="0.6" />
                            </DataTemplate>
                        </xctk:WatermarkTextBox.WatermarkTemplate>
                    </xctk:WatermarkTextBox>
                </StackPanel>
            </Border>
            <Grid Grid.Row="2" Grid.ColumnSpan="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Content="Добавить" Grid.Column="0" Foreground="White"/>
                <Button Content="Отмена" Click="Button_Click" Grid.Column="2" Foreground="White"/>
                <Button x:Name="SendButton" Content="Отправить" Click="SendButton_Click" Grid.Column="3" Foreground="White"/>
            </Grid>
        </Grid>
    </Border>
</Window>
