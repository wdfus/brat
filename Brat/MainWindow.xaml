<Window x:Class="Brat.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Brat" Height="1000" Width="1800" WindowStartupLocation="CenterScreen"
        Background="#2D2D2D">
    <Window.Resources>
        <!-- Палитра -->
        <SolidColorBrush x:Key="Col.AppBg"     Color="#2D2D2D"/>
        <SolidColorBrush x:Key="Col.Rail"      Color="#5A5A5A"/>
        <SolidColorBrush x:Key="Col.Side"      Color="#3A3A3A"/>
        <SolidColorBrush x:Key="Col.Top"       Color="#444444"/>
        <SolidColorBrush x:Key="Col.Input"     Color="#444444"/>
        <SolidColorBrush x:Key="Col.BubbleL"   Color="#E6E6E6"/>
        <SolidColorBrush x:Key="Col.BubbleR"   Color="#0078D7"/>

        <!-- Иконка-кнопка (круг) -->
        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="#505050"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#6A6A6A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="18"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Макет одного элемента чата -->
        <DataTemplate x:Key="ChatItemTemplate">
            <Grid Margin="10,6,10,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="42"/>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Ellipse Width="36" Height="36" Fill="#D9D9D9" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                    <StackPanel Height="10" Width="216">
                        <TextBlock Text="ТЕКСТ" FontSize="8" FontWeight="Bold" Foreground="White"></TextBlock>
                    </StackPanel>
                    <Rectangle Height="8"  Width="120" RadiusX="4" RadiusY="4" Fill="#C8C8C8" HorizontalAlignment="Left" Margin="0,6,0,0"/>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <!-- Пузырьки -->
        <Style x:Key="BubbleLeft" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource Col.BubbleL}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
        <Style x:Key="BubbleRight" TargetType="Border" BasedOn="{StaticResource BubbleLeft}">
            <Setter Property="Background" Value="{StaticResource Col.BubbleR}"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>

        <!-- Убираем выделение у ListBoxItem (скелет-лист) -->
        <Style TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Focusable" Value="False"/>
        </Style>
    </Window.Resources>

    <!-- ===== ГЛАВНАЯ СЕТКА: 3 колонки (рейл / чаты / сообщения) и 3 строки ===== -->
    <Grid Background="{StaticResource Col.AppBg}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="64"/>
            <!-- узкий рейл -->
            <ColumnDefinition Width="300"/>
            <!-- список чатов -->
            <ColumnDefinition Width="*"/>
            <!-- область переписки -->
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <!-- верхняя плашка -->
            <RowDefinition Height="*"/>
            <!-- контент -->
            <RowDefinition Height="60"/>
            <!-- нижняя плашка ввода (только для переписки) -->
        </Grid.RowDefinitions>

        <!-- ВЕРХНЯЯ ПАНЕЛЬ — на всю ширину -->
        <Border Grid.Row="0" Grid.ColumnSpan="3" Background="{StaticResource Col.Top}">
            <Grid Margin="16,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Заголовок чата" Foreground="White" VerticalAlignment="Center" FontWeight="Bold"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource IconButton}" Content="…"/>
                    <Button Style="{StaticResource IconButton}" Content="≡"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ЛЕВЫЙ РЕЙЛ -->
        <Grid Grid.Row="1" Grid.Column="0" Background="{StaticResource Col.Rail}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Margin="12,12,12,0">
                <Ellipse Width="40" Height="40" Fill="#D9D9D9"/>
                <TextBlock Text="Я даун" Foreground="White" Margin="0,8,0,0" FontWeight="SemiBold"/>
            </StackPanel>
            <Button Grid.Row="2" Style="{StaticResource IconButton}" Content="★" Margin="12" Background="#4F4F4F"/>
        </Grid>

        <!-- КОЛОНКА СО СПИСКОМ ЧАТОВ -->
        <Grid Grid.Row="1" Grid.Column="1" Background="{StaticResource Col.Side}">
            <Grid.RowDefinitions>
                <RowDefinition Height="56"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Поиск -->
            <Border Margin="12" Background="#5A5A5A" CornerRadius="20" Height="36" VerticalAlignment="Center">
                <Grid Margin="12,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="24"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Ellipse Fill="#9E9E9E" Width="14" Height="14" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" VerticalAlignment="Center" Foreground="#E5E5E5" Text="Найти" FontSize="14"/>
                </Grid>
            </Border>

            <!-- Макеты элементов чатов -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ListBox Background="Transparent" BorderThickness="0" ItemTemplate="{StaticResource ChatItemTemplate}" SelectionChanged="ListBox_SelectionChanged" HorizontalAlignment="Center">
                    <!-- 12 «болванок» -->
                    <sys:String>1</sys:String>
                </ListBox>
            </ScrollViewer>
        </Grid>

        <!-- ОБЛАСТЬ СООБЩЕНИЙ -->
        <Grid Grid.Row="1" Grid.Column="2" Background="White">
            <ScrollViewer Padding="24,16">
                <StackPanel>
                    <Border Style="{StaticResource BubbleLeft}">
                        <TextBlock Text="Привет!" Foreground="Black"/>
                    </Border>
                    <Border Style="{StaticResource BubbleLeft}">
                        <TextBlock Text="Как дела?" Foreground="Black"/>
                    </Border>

                    <!-- Разделитель «Непрочитанные сообщения» -->
                    <Border Background="#666666" Height="24" Margin="0,12">
                        <TextBlock Text="Непрочитанные сообщения" Foreground="White"
                                   FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>

                    <Border Style="{StaticResource BubbleRight}">
                        <TextBlock Text="Все отлично!" Foreground="White"/>
                    </Border>
                    <Border Style="{StaticResource BubbleLeft}">
                        <TextBlock Text="Погнали дальше макет делать." Foreground="Black"/>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <!-- НИЖНЯЯ ПАНЕЛЬ ВВОДА — только под областью сообщений -->
        <Border Grid.Row="2" Grid.Column="2" Background="{StaticResource Col.Input}">
            <Grid Margin="12,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="56"/>
                </Grid.ColumnDefinitions>
                <Border Background="#5A5A5A" CornerRadius="8" Height="40" VerticalAlignment="Center" Margin="0,0,8,0">
                    <TextBox BorderThickness="0" Background="Transparent" Foreground="White"
                             Padding="12,8" FontSize="14" Text="Сообщение..."/>
                </Border>
                <Button Grid.Column="1" Content="➤" FontSize="18" FontWeight="Bold"
                        Background="#5A5A5A" Foreground="White" BorderThickness="0"
                        Width="56" Height="40" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
